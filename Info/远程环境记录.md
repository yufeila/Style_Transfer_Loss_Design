# 远程环境记录

记录日期：2026-06-18

## 连接与路径

- 远程连接命令：`ssh gpu-server`
- 远程主机：`ubuntu`
- 默认登录目录：`/home/yyf`
- 远程工作区：`/data1/yyf/Style_Transfer_Loss_Design`
- 本地工作区：`/Users/yufei/course/third_year_sem2/AI_class/Final project/Style_Transfer_Loss_Design/Experiments`

## 远程目录

已创建并确认：

```text
/data1/yyf/Style_Transfer_Loss_Design/
  code/
  data/
  outputs/
  logs/
  models/
```

## Conda 环境

- Miniconda 路径：`/workspace/yyf/env/miniconda3`
- conda 版本：`conda 26.1.1`
- 项目环境：`/workspace/yyf/env/conda/style-transfer`
- Python 版本：`Python 3.10.20`

激活命令：

```bash
source /workspace/yyf/env/miniconda3/etc/profile.d/conda.sh
conda activate /workspace/yyf/env/conda/style-transfer
```

## GPU 状态

远程服务器有 4 张 NVIDIA A800 80GB PCIe。

检查时状态：

```text
GPU 0: 7281 MiB / 81920 MiB, utilization 98%
GPU 1: 4 MiB / 81920 MiB, utilization 0%
GPU 2: 4 MiB / 81920 MiB, utilization 0%
GPU 3: 69331 MiB / 81920 MiB, utilization 0%
```

优先使用 GPU 1 或 GPU 2。

## 磁盘状态

检查时状态：

```text
/data1: 7.0T total, 6.0T available
/workspace: 7.0T total, 5.8T available
```

## 网络状态

- `https://pypi.org` 可直接访问。
- `https://github.com` 直接访问 10 秒超时。
- 后续如需克隆 GitHub 或下载 GitHub release，优先考虑本地代理 + SSH 反向隧道。
